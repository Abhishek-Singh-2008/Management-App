{
  "name": "mini-project-management-workspace",
  "version": "1.0.0",
  "description": "Workspace for Mini Project Management App",
  "private": true,
  "scripts": {
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run dev --prefix frontend\" --names \"Backend,Frontend\" --prefix-colors \"magenta,cyan\"",
    "test": "npm run test --prefix backend",
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js"
  }
}
